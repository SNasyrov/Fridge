@model FridgeV2.Models.ShoppingListItem
@{
    ViewBag.Title = "Удалить продукт";
}

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.AmountOfProduct)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.AmountOfProduct)
        </dd>
    </dl>

    <div>
        <form asp-controller="ShoppingListItems" asp-action="Delete" method="post">
            <div class="form-group">
                <input type="submit" class="btn btn-outline-danger" value="Удалить" />
            </div>
        </form>
    </div>
</div>